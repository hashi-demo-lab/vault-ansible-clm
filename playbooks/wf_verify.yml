---
# playbooks/wf_verify.yml
# AAP Workflow Node: Verify TLS handshake and certificate deployment.

- name: "WF: Verify Certificate"
  hosts: "{{ target_hosts | default('all') }}"
  serial: 1
  gather_facts: true

  tasks:
    - name: "TEMPORARY: Force failure to test rollback path"
      ansible.builtin.fail:
        msg: "Intentional failure to test workflow rollback path"
